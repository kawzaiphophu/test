export interface CategoriesType {
    categoryId?: number,
    categoryName?: string,
    images?: string,
    description?: string,
    status?: string,
    createdBy?: string,
    updatedBy?: string
}